class BookModel {
  String id;
  String bookname;
  String bookdesc;
  String authorname;
  String publishdate;
  int rate;
  String bookurl;

  BookModel(this.id, this.bookname, this.bookdesc, this.authorname,
      this.publishdate, this.rate, this.bookurl);
}
